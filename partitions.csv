# The default single-app table caps the app at 1MB, which WiFi + mbedTLS and
# the full certificate bundle overflow. The board has 16MB, so give the app 4MB.
# Name,     Type, SubType, Offset,  Size,
nvs,        data, nvs,     0x9000,  0x6000,
phy_init,   data, phy,     0xf000,  0x1000,
factory,    app,  factory, 0x10000, 4M,
